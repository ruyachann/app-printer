# Printer Protocol 仕様書（Phase 10, 13-14）

通信解析の結果を集約したプロトコル仕様書です。Python実装（`printer/`配下）はこの仕様に基づいて行います。
**不明な項目は推測で埋めず、「未確定」と記録すること。**

## Connection（確定）

| 項目 | 値 |
|---|---|
| Bluetooth方式 | **Classic（RFCOMM/SPP）**。BLE/GATTではない |
| Service UUID / Write Characteristic UUID | 該当なし（BLEでないため） |
| トランスポート | RFCOMM（DLCI=0が制御チャネル、DLCI=2がデータチャネル） |
| Write Type | RFCOMM UIHフレーム |

詳細は `docs/bluetooth_gatt.md` を参照。

## Packet 構造（暫定・詳細解析中）

各印刷ジョブは、RFCOMM DLCI=2 のUIHフレーム列として送信される。ジョブ全体の構造:

| フィールド | サイズ | 内容 |
|---|---|---|
| Header | 8 bytes | `1D 47 59 04 30 00 17 00`（全ジョブ共通、固定値。各バイトの意味は未解析） |
| Image Payload | 可変（4400〜5600 bytes程度） | 大半が0x00、一部非ゼロの1bitラスタービットマップと推定。幅・高さ・ビット順は未解析 |
| Footer | 可変 | `1B 4A 50 ...` を含む（ESC/POSの `ESC J n` (行送り)に類似）。詳細未解析 |
| Checksum | 未確定 | 未確認 |

ジョブ間には短い制御フレーム（`01 10 ff` 等）が挟まることがある（ステータス確認と推定、未解析）。

生データは `captures/rfcomm_jobs/job_00.bin` 〜 `job_17.bin` に保存済み。

## 文字（Text）

| 項目 | 内容 |
|---|---|
| 文字コマンドが存在するか | 未確定 |
| 文字コード（存在する場合） | 未確定 |
| 画像化されているか | 未確定（Phase 9-10の比較結果より判定） |

## QRコード

| 項目 | 内容 |
|---|---|
| 専用QRコマンドの有無 | **再検証中**。当初「アプリにQR機能なし」と判断したが、実際には「ノートエディタ」の挿入メニュー内にQR/バーコード機能が存在することが判明。実際にQR印刷し通信を比較する必要がある |
| QRコマンドの構造（存在する場合） | 未確認（再検証中） |
| 画像化されている場合の変換フロー | 文字印刷が疎な1bitビットマップとして送信されていることが確認されているため、QRも同じ画像プロトコルに乗せて送られている可能性が高い。ただし専用コマンドの可能性も再検証する |

## Image

| 項目 | 値 |
|---|---|
| Width | 未確定 |
| Height（可変か固定か） | 未確定 |
| Format | 未確定（1bit monochrome / 8bit grayscale 等） |
| Byte order | 未確定（LSB first / MSB first） |
| 白黒反転の有無 | 未確定 |
| 横方向ビット順 | 未確定 |
| 縦方向ビット順 | 未確定 |

## パケット分割（BLE分割送信）

| 項目 | 値 |
|---|---|
| 1パケット最大サイズ | 未確定 |
| 分割単位 | 未確定 |
| シーケンス番号の有無 | 未確定 |
| 終端マーカー | 未確定 |
| チェックサム | 未確定 |
| ACK/Notificationの有無 | 未確定 |

## 画像開始/終了コマンド

| 項目 | 値 |
|---|---|
| 画像開始コマンド | 未確定 |
| 画像終了コマンド | 未確定 |

## 判定結論（重要）

- Bluetooth方式: **Classic（RFCOMM/SPP）で確定**。BLEではない
- 文字は画像化されているか: **画像化の可能性が非常に高い（ほぼ確定）**。全テキスト印刷ジョブが同一ヘッダーで開始し、ペイロードが疎な1bitビットマップパターンを示す。ジョブごとのバイト長の違いも文字列の長さ・複雑さと相関している可能性が高い（例: job_02が5633 bytesと突出して大きい）
- QRは専用コマンドか画像化か: **再検証中**（ノートエディタ内にQR/バーコード挿入機能を発見。実際にQR印刷して通信比較が必要）
- 採用方式（手順書 30章の優先順位に基づく）: 文字は画像プロトコルで送信されている可能性が非常に高い。QRについても同じ画像プロトコルに乗る可能性が高いが、キャプチャで確認する。ヘッダー/フッターのバイト単位の意味・幅・高さ・チェックサムは未解析で、今後の作業項目とする
