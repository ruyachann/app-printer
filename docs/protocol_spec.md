# Printer Protocol 仕様書（Phase 10, 13-14）

通信解析の結果を集約したプロトコル仕様書です。Python実装（`printer/`配下）はこの仕様に基づいて行います。
**不明な項目は推測で埋めず、「未確定」と記録すること。**

2026-08-26時点: ヘッダー・ビットマップ構造・幅・高さ・ビット順は **バイトレベルで解読済み**。
実際のビットマップをレンダリングして目視確認済み（`captures/rendered/contact_sheet.png`）。

## Connection（確定）

| 項目 | 値 |
|---|---|
| Bluetooth方式 | **Classic（RFCOMM/SPP）**。BLE/GATTではない |
| Service UUID / Write Characteristic UUID | 該当なし（BLEでないため） |
| トランスポート | RFCOMM（DLCI=0が制御チャネル、DLCI=2がデータチャネル） |
| Write Type | RFCOMM UIHフレーム |

詳細は `docs/bluetooth_gatt.md` を参照。

## 印刷ジョブの全体構造（確定）

各印刷ジョブ（文字・QRとも共通）は、RFCOMM DLCI=2 のUIHフレーム列として送信される、以下の3部構成:

```text
[Header 8 bytes] + [Bitmap payload (height*48 bytes)] + [Footer]
```

生データは `captures/rfcomm_jobs/` に保存済み（`job_00.bin`〜`job_17.bin`＝文字、`qr_ABC123.bin`／`qr_url.bin`＝QR）。
レンダリング結果は `captures/rendered/` を参照（全ジョブの内容を目視確認済み）。

### Header（8 bytes、確定）

| オフセット | サイズ | 内容 |
|---|---|---|
| 0-4 | 5 bytes | 固定値 `1D 47 59 04 30`（コマンド識別子。意味は未解析だが全ジョブ共通） |
| 5 | 1 byte | 固定値 `00`（未解析、常に0） |
| 6-7 | 2 bytes | **高さフィールド（リトルエンディアン u16）。単位は「4ピクセル行」**。実際の行数 = この値 × 4 |

検証: 文字ジョブは全て高さフィールド=23 → 実行数=92行。QR(ABC123)は295→1180行、QR(URL)は238→952行。
いずれも `(実データ長 - 8) ÷ 48` と完全一致し、幅48バイト固定という仮説を裏付けている。

### Bitmap Payload（確定）

| 項目 | 値 |
|---|---|
| Width | **384 px = 48 bytes/row（1bit per pixel）** |
| Height | ヘッダーの高さフィールド × 4（可変。文字は常に92行固定キャンバス、QRは内容により変動） |
| Format | 1bit monochrome |
| Byte order（横方向ビット順） | **MSBファースト**（バイトの最上位ビットが左端のピクセル） |
| 白黒 | **bit=1 → 黒（インク）、bit=0 → 白（紙地）** |
| 縦方向 | 先頭バイトが画像の一番上の行 |

文字は常に384×92の固定キャンバスに描画され、内容（"A"〜"あいうえお"等）によって黒ピクセルの位置のみが変わる。
QRコードは内容の複雑さに応じて高さフィールドが変動する（例: 短い文字列で295、長いURLで238。QRのバージョン/モジュール数に依存すると推測される。単純な文字数比例ではない）。

### Footer（一部確定）

| 項目 | 値 |
|---|---|
| 先頭バイト列 | `1B 4A 50 ...`（ESC/POSの `ESC J n`＝紙送りコマンドに類似した構造） |
| 詳細 | 続くバイト列の正確な意味（チェックサム有無等）は未解析 |

ジョブ間には短い制御フレーム（`01 10 ff` 等）が挟まることがある（ステータス確認/フロー制御と推定、詳細未解析）。

## 文字（Text）

| 項目 | 内容 |
|---|---|
| 文字コマンドが存在するか | **なし**。専用の文字コード送信コマンドは存在しない |
| 画像化されているか | **確定・目視確認済み**。「A」「B」「AB」「ABC」「Hello」「1234567890」「あ」「あいうえお」「ABC123」「URL文字列」全てが、上記ビットマップフォーマットで正しくレンダリングされることを確認した |

## QRコード

| 項目 | 内容 |
|---|---|
| 専用QRコマンドの有無 | **なし**。ノートエディタの挿入機能でQRを作成しても、上記と全く同じビットマップフォーマットで送信される（目視確認済み: `captures/rendered/qr_ABC123.png`, `qr_url.png`） |
| 画像化されている場合の変換フロー | Python側で `qrcode` ライブラリによりQR画像を生成し、上記ビットマッププロトコルに変換して送信する |

## Image（文字・QRのキャプチャ結果から導出。写真等の複雑な階調画像は未検証）

| 項目 | 値 |
|---|---|
| Width | **384 px（確定）** |
| Height（可変か固定か） | 可変（4の倍数、ヘッダーで指定） |
| Format | **1bit monochrome（確定）** |
| Byte order | **MSBファースト（確定）** |
| 白黒反転の有無 | **なし。bit=1が黒（確定）** |
| 横方向ビット順 | MSBファースト（確定） |
| 縦方向ビット順 | 先頭行から順（確定） |

写真のような多階調画像を送る場合、Pillow等で2値化（ディザリング推奨）してから同じ1bitフォーマットに変換する想定。

## パケット分割（RFCOMM）

| 項目 | 値 |
|---|---|
| 1パケット最大サイズ | 約240 bytes（RFCOMM UIHフレームのペイロード、MTU起因と推定） |
| 分割単位 | 240 bytes単位で単純分割（ヘッダー・ビットマップ・フッターを連続バイト列とみなし、240バイトごとに区切って送信） |
| シーケンス番号の有無 | 確認できず（なしと推定） |
| 終端マーカー | フッターの `1B 4A 50` 以降を参照 |
| チェックサム | 未確定 |
| ACK/Notificationの有無 | RFCOMM自体がACK付き信頼性転送のため、アプリケーション層でのACKは不要と推定 |

## 画像開始/終了コマンド

| 項目 | 値 |
|---|---|
| 画像開始コマンド | ヘッダー `1D 47 59 04 30 00 [height_LE_u16]` |
| 画像終了コマンド | フッター `1B 4A 50 ...`（用紙送り相当） |

## 既知の未解決問題（重要）: 自前生成した画像データが4分割されて印字される

**症状:** 実機（Windows PC、COM4経由）で、`printer/`ライブラリを使って**自分で生成した**画像データ
（文字・QR・手作業で作った模様など）を送信すると、紙面に**横に4分割された状態**で印字される
（同じ内容が均等間隔で4回並ぶ）。一方、`captures/rfcomm_jobs/`の**実キャプチャデータをそのまま
再送信した場合は、常に正しく1つだけ**印字される。

### 調査で切り分けた事実（2026-08-26、実機テストにて）

| 検証内容 | 結果 |
|---|---|
| 送信経路（`PrinterConnection.connect()`/`write()`、チャンク分割、チャンク間ディレイ） | **正常**。実データを流すと問題なく動作する |
| `protocol.build_print_command()`（ヘッダー・フッター生成ロジック） | **正常**。実キャプチャの中身を渡すと実キャプチャとバイト単位で完全一致し（`tests/test_protocol_replay.py`）、実機でも正しく1つだけ印字される |
| 画像の総バイト数・行数（52行〜1000行超まで変化） | **無関係**。どのサイズでも自前生成データは4分割される |
| 0xFFバイト（8ピクセル全黒）の有無 | **無関係と判明**。0xFFを減らしても解決せず |
| データの複雑さ・規則性（単色/市松模様/斜め線/ランダムノイズ） | **無関係と判明**。ランダムノイズでも4分割される |
| PIL/qrcodeライブラリ経由か手作業で作ったバイト列か | **無関係**。PILを一切使わない手作業データでも再現する |
| 実データと同じ92行（本物の「A」と同サイズ）にしても | **解決せず**。サイズを合わせても自前生成データは4分割される |
| 本物の「A」データの空白部分は残し、文字部分だけ自前の模様に差し替え | **4分割される**。周囲の構造（空白配置）ではなく、書き換えた中身自体が原因と判明 |

### 追加調査（続報、2026-08-26）: 384px幅は実は4つの「96px区画」に分かれている

その後の実機テストで、さらに重要な手がかりが得られた。

1. **本物のアプリの「実データ」を詳細に解析したところ、黒（インク）部分は実は真っ黒(0xFF)ではなく、
   全体で黒画素比率わずか約8.6%程度の疎な網目模様（ハーフトーン）で構成されていた。** 均一な塗りつぶし
   は一切使われていない。
2. ノートエディタで**実際に「QRコードを4つ手動で横に並べて」印刷**してもらい、そのキャプチャを解析
   したところ、この4つのQRコードは1つのジョブ（384×348px）としてまとめて送信されており、
   問題なく正しく印字されることを確認した（実データはやはり複雑な内容でも常に正常動作する）。
3. **「左半分だけ黒」のシンプルな画像を送信したところ、紙面には「左右に均等な間隔で4つの小さな黒い
   四角」が印字された。** これは、1枚の画像全体が縮小されて4回複製されているように見える結果であり、
   単純な「バイト列の断片化」では説明が付かない。

これらから、**このプリンターは384px幅を4つの「96px区画」に物理的に分割して処理しており、
自前生成したデータ（384px幅の1本の連続したビットマップ行として送信）は、その4区画それぞれに
「圧縮コピー」されてしまっている**と考えられる。おそらく本物のアプリは、384px幅いっぱいの
内容を4区画それぞれに向けて個別にフォーマットして送信しているが、その正確なデータ配置方法
（インターリーブなのか、区画ごとの個別ヘッダーがあるのか等）はまだ特定できていない。

文字印刷（`print_text`）が実用上問題なく見えるのは、生成される文字画像の幅が384px全体ではなく
比較的狭い範囲に収まっていることが多く、4分割・圧縮されても視覚的な破綻が目立ちにくいためと
推測される。

### 結論

原因は「本物のアプリが生成したデータの区画別配置方法」と「私たちの単純な1本のビットマップ行と
しての送信方法」の間の違いにあると強く推測されるが、正確な配置ルール（各96px区画ごとにデータを
どう並べて送るか）はまだ解明できていない。サイズ・0xFF有無・パターンの規則性は決定要因ではないと
確認済みで、密度（ハーフトーン処理）を実データに寄せる試みでも完全解決には至っていない。

### 影響範囲

- **文字印刷（`print_text`）**: 実際に読める大きさで印刷できることを確認済みだが、幅の狭い文字は
  4分割の影響が視覚的に目立ちにくいだけで、理論上は同じ問題を抱えている可能性が高い
- **QR印刷（`print_qr`）**: 4分割されて出力され、実用上スキャンが困難。**未解決の既知の問題**
- **画像印刷（`print_image`）**: 同様の問題が起きる可能性が高いが未検証

### 引き継ぎ時に試すと良さそうなこと

- **最優先:** 本物のアプリに、384px幅いっぱいを使う「左右で内容が異なる」コンテンツ（例:
  長い文字列やノートエディタでの複数要素配置）を印刷させ、そのキャプチャを解析して、
  4つの96px区画それぞれにどうデータが配置されているか（インターリーブ方式なのか、
  区画ごとに独立したヘッダーがあるのか等）を特定する
- 実機のメーカー・型番（GKE Mini Pocket Printer S1-A20, FW V3.1.4）から公式SDK/開発者向け資料が
  入手できないか確認する（本調査は公式資料なしでの独自解析のため、正式仕様と食い違う可能性がある）
- 実キャプチャデータ（`captures/rfcomm_jobs/`）を「テンプレート」として、複数の実キャプチャ文字を
  組み合わせて任意の短い文字列を合成する回避策（本格的な原因究明ではないが実用上の代替手段）
- ロジックアナライザ等でBluetooth通信を直接モニタリングしながら、生成データ送信時と実データ送信時の
  タイミング・応答（Notification等）の違いを比較する

### 決定的な追加発見（2026-08-26 続報2）: ビットマップの各バイトは実は256種類ではなく16種類の値しか使われていない

上記の「4区画」仮説を検証する過程で、`captures/rfcomm_jobs/`配下の**全実キャプチャ（文字18件＋
QR単体2件＋4連結QR実データ、計198,528バイト）のビットマップ本体を1バイトずつ統計調査**したところ、
決定的な事実が判明した。

**実際に出現するバイト値は、256通り中わずか16通り（`0x00,0x01,0x02,0x03,0x10,0x11,0x12,0x13,
0x20,0x21,0x22,0x23,0x30,0x31,0x32,0x33`）のみ。それ以外の240種類の値（`0xFF`はもちろん、
`0x04`〜`0x0F`, `0x40`〜`0xFF`等）は、内容の複雑さ・密度を問わず、実データ中に一度も出現しない。**

これは「上位ニブル・下位ニブルとも、常に値が0〜3の範囲に収まる」と言い換えられる。ビット単位で見ると、
1バイト＝8ビット（bit7が1画素目、bit0が8画素目、MSBファースト）のうち、
**bit7・bit6・bit3・bit2の4ビット（画素位置でいうと1,2,5,6番目）は常に0固定で、
bit5・bit4・bit1・bit0（画素位置3,4,7,8番目）だけが値を持つ**、という規則的なパターンになっている
（4画素ごとに「常時白2画素＋可変2画素」を繰り返す構造）。

この発見が重要な理由:
1. **統計的に偶然ではあり得ない。** 198,528バイトがランダムなら`0xFF`等の「稀な」値も0個ということは
   まず起こらない（期待値は約780回）。何らかの明確なフォーマット上の制約が存在する。
2. **QRコードの位置検出パターン（ファインダーパターン、7×7モジュールの塗りつぶし四角）は本来
   連続した黒画素の塊のはずだが、実データ中に`0xFF`（8画素連続黒）が一度も出現しない。**
   つまり本物のアプリは、単純な1bit（黒/白）のベタ塗りを一切送信しておらず、**常に何らかの
   ハーフトーン処理（疑似階調表現）を経たデータだけを送信している**と考えられる（既出の
   「黒画素比率8.6%」という観察とも整合する）。
3. **私たちが`printer/image.py`・`printer/qr.py`で生成しているビットマップは、この16値の
   制約を一切考慮しておらず、`0x00`〜`0xFF`の256通りを自由に使っている。** これは実データの
   「取りうる値の範囲」から完全に外れており、4分割バグの直接的な原因である可能性が高い
   （未知のバイト値を受け取ったファームウェアが、想定外の処理・エラー回復パスに入り、
   その結果が「4分割複製」として観測されている、という仮説が成り立つ）。

ただし、この16値制約を「画素データとしてどう正しく解釈すべきか」（1バイトに2画素だけ実在するのか、
2bit階調値が2個埋め込まれているのか等）はまだ確定していない。試しに「1バイト＝2個の2bit階調値
（96px幅相当）」として実データをデコードしてみたところ、文字「A」はほぼ正しい縦横比で復元できた
一方、QRコードは縦横比が大きく崩れる結果になり、この解釈だけでは説明が付かない部分が残っている
（解像度・行の意味についても再検証が必要）。

### 次に実機で試すべき最有力の実験 → 実施済み・結果は否定的（2026-08-26 続報3）

`tools/experiments/mask_test.py`で生成QRの各バイトに`0x33`マスクを掛け、実データと同じ
「16値ドメイン」に収めた状態で実機テストを行った。**結果: 4分割は解消しなかった**
（依然として横に4つ、かつマスクで情報量が減った分サイズも小さくなった状態で印字された）。

これにより、**「バイト値が256通り自由に使われていること」単体が4分割の直接原因ではない**と判定した。
ただし、実データが16値ドメインに収まっているという統計的事実自体は揺るがない（何らかの
ハーフトーン/量子化処理の結果と考えられる）。4分割の原因は依然として未特定。

### 追加の再検証（2026-08-26 続報3）: 4連結QR実データの再レンダリングで新たな疑問

`job4up_native.bin`（本物のアプリで「QRコードを4つ横に並べた」ノートを印刷した実データ）を、
従来の1bit/384px幅の解釈でレンダリングし直したところ、期待していた「横に4つ並んだQRコード」
ではなく、**384px幅・348行の画像を縦方向に3等分したあたりに、似たような密度の模様が
繰り返し現れる**という、想定と異なる構造が見えた（`captures/rendered/job4up_native.png`）。
94px×4列の横並びとしては綺麗に解釈できていない。これは以下のいずれかを意味する可能性がある。

- 1bit/384px幅というレンダリング前提そのものが、複雑な実データに対しては不正確
- ノートの実際のレイアウトが「横4つ」ではなく、想定と異なる配置だった
- ハーフトーン処理された密な模様が、レンダリング解像度の制約で単に判読しづらいだけ

この点は完全には解消できておらず、次の調査者が実データを再検証する際の注意点として記録する。

### 続報4（2026-08-26）: print_image()の実機初テストと「実効幅96px」仮説

`print_image()`は本調査開始以来一度も実機テストされていなかったため、384px幅いっぱいを使う
左右非対称の診断用画像（`captures/test_assets/asymmetric_test.png`：左に円(x10〜100)、
中央に縦線(x188〜196)、右に三角形(x284〜374)）を初めて実機送信した。

**結果:** 紙面には「円｜三角形+円｜三角形+円｜三角形+円｜三角形」という、区切り線を挟んで
5つの区画が並び、円単体・三角形単体はそれぞれ両端の区画にのみ現れ、中間の3区画では
三角形と円が重なって現れるという、非常に規則的なパターンが観測された。

各要素のx座標を96で割った余り（mod 96）を計算すると、この観測結果とよく一致することが分かった。

| 要素 | 元のx範囲 | mod 96 |
|---|---|---|
| 円 | 10〜100 | 10〜4（96px境界をまたいで折り返る） |
| 縦線 | 188〜196 | 92〜4（同上、区画の境界付近に来る） |
| 三角形 | 284〜374 | 92〜86（ほぼ区画全幅に折り返って広がる） |

縦線が区画境界付近（mod値92〜4）に来ることは、印字結果で線が「区画の区切り」のように見えたことと
一致し、三角形が折り返りでほぼ区画全幅に広がることは、三角形が各区画で大きく・目立って見えたことと
一致する。これらから、**プリンターの実効印字幅は384pxではなく96pxであり、96pxを超える列位置の
データは何らかの形で周期的に折り返されて（あるいは96px単位で区画ごとに独立して）処理されている**
という仮説を新たに立てた（ただし完全な周期関数であれば4区画は全く同一に見えるはずだが、実際には
両端の区画だけ内容が異なっており、単純な無限周期の折り返しだけでは説明が付かない部分も残る）。

この仮説を検証するため、`tools/experiments/narrow_qr_test.py`を作成した。QRコードを**96px以内**
に収まるサイズで生成し、384pxキャンバスの残り（96px〜383px）を完全に空白のまま送信するスクリプト
である。この状態で1個だけ正しいQRコードが印字されれば、「実効幅96px」仮説が強く支持される。

**→ 実施結果: 96px以内に収めても、依然として非常に小さいQRコードが4つ印字された。**
これ自体は「実効幅96px」仮説と矛盾しない（周期96pxの単純な繰り返しなら、96px以内に収まる
内容はどのみち4回現れるはず）が、決定的な証拠にはならなかった。

### 続報5（2026-08-26）: 決定的な訂正 — 「区画ごとに別内容」ではなく「画像全体の縮小コピー」

数字較正用画像（384px幅を8等分し、各48px区画に1〜8の数字を1つずつ配置）を実機テストしたところ、
紙面には**「12345678」という元の並び全体がそのまま4回連続で繰り返されて**印字された
（区画ごとに異なる数字が現れる、という予想は外れた）。これにより、続報4での「三角形が区画内で
円と重なる」という解釈は誤りだったと判明した（実際には、単に1回分のコピーの末尾＝三角形と、
次のコピーの先頭＝円が隣接して見えていただけだった）。

**訂正後の結論:** プリンターは受信した384px幅の画像**全体**を約1/4の幅に縮小し、それを4回
横に並べて印字している。区画ごとに異なるデータを読んでいるのではなく、単純に「画像全体の
縮小コピーを4つタイル状に並べる」という動作である。この動作は、これまで試したあらゆる
自前生成データ（文字・QR・図形・数字、あらゆるサイズ・density・バイト値ドメイン）に対して
一貫して発生しており、**条件によらず常に発生する固定的な動作**に見える。

### 続報5・追加: 「行数のしきい値」仮説も否定

続報4の「高さが実データ並みに大きければ4分割が止まるのでは」という仮説を検証するため、
数字較正画像を白紙で1200行まで延長した`position_calibration_tall.png`（実データのQR
[qr_ABC123.bin：1180行]とほぼ同じ規模）を実機テストした。**結果: 依然として「12345678」が
4回繰り返された**（文字サイズは変わらず、左右の余白バランスがわずかに変化した程度）。
これにより「行数が少ないと自動的に4分割モードになる」という仮説は棄却した。

### 現時点のまとめ（続報5時点）

以下の要因はすべて個別に切り分け、いずれも単独の決定要因ではないと確認済み:

- データサイズ・行数（52行〜1200行超）
- 高さ（実データ規模の1200行に揃えても無関係）
- 0xFFバイト・バイト値のドメイン（16値に制限しても無関係）
- パターンの複雑さ・規則性（単色/市松模様/ランダムノイズ/数字/幾何学図形すべてで再現）
- 送信幅（96px以内に収めても再現）

一方で、実キャプチャデータの**バイト列をそのまま**（＝内容を一切変更せず）送信した場合は、
常に正しく1回だけ印字される。この事実と上記の網羅的な切り分け結果を総合すると、
**4分割動作を止めているのは、実データの「特定の統計的性質」ではなく、実データの
バイト列そのものに含まれる何らかの情報（本物のエンコーダにしか作れない、単純な統計量では
再現できない構造）である可能性が高い**と考えられる。単純な1bit生ビットマップという前提
そのものを疑い、実際には何らかの圧縮・エンコード方式が使われている可能性も含めて、
今後は「内容が一部だけ異なる複数の実データを比較する」（例: URLが1文字だけ違うQRコードを
2つ実際に印刷させ、両者のキャプチャをバイト単位で比較する）という差分解析アプローチが
最も有望と考えられる。

### 続報6（2026-08-26）: 差分解析・マージン仮説・送信経路の再検証

**差分解析:** URLが1文字だけ違うQRコードを2つ実際に印刷させ、キャプチャをバイト単位で
比較した。差分は約10.4%のバイトに及んだが、差分の分布は「QRコードの誤り訂正符号の影響で
内容変更が複数モジュールに波及する」という通常のQR特性で説明できる範囲にとどまり、新しい
プロトコル構造は見つからなかった（バイト値が16種類のドメインに収まる、という既存の発見は
この新データでも再確認できた）。

**決定的な発見: `print_text()`も実は4分割されていた。** 「12345678」を本物のアプリで印刷すると
1行で正しく印字される一方、この文字列を長さ4倍に連結した文字列（本物のアプリがフォントを
自動縮小して1行に収めた）も正しく1回だけ印字された。これに対し、同じ内容を`print_text()`
（文字専用の関数、QR/画像とは別のコードパス）で送ったところ、**文字は大きく読みやすくなったが
依然として4回現れた**。これにより、「文字印刷は正常に動作している」という、調査序盤からの
前提が実は誤りだったと判明した。文字・QR・画像、**このライブラリが生成するあらゆるビットマップが
例外なく同じ4分割症状を起こしている**可能性が高い（過去の「文字はきちんと印字できた」という
報告は、初期のフォントサイズが小さかったため、4つに圧縮複製された文字が一見「小さい一つの文字列」
に見えていただけだった可能性がある。実際、調査最初期の「４つの塊(線)があるように見える」という
観察は、今にして思えば4分割症状そのものだったと考えられる）。

**送信経路の再検証:** 上記の発見により「原因はビットマップの中身ではなく送信経路（pyserial経由の
COMポート接続への切り替え）ではないか」という新しい疑いが浮上した。実データ（`job_00.bin`,
「A」1文字）を、現在の`PrinterConnection`（Windows COMポート/pyserial経由）を使って
そのまま再送信するテスト（`tools/experiments/replay_test.py`）を実施したところ、
**「A」が正しく1つだけ印字された。** これにより、現在の送信経路自体は問題ないと確定し、
原因は依然として「ビットマップの中身」にあると判断した。

**現時点の理解:** サイズ・行数・バイト値ドメイン・パターンの規則性・送信幅・余白（マージン）・
送信経路など、思いつく限りの要因を切り分けたが、いずれも単独の決定要因ではなかった。
文字・QR・画像を問わず、このライブラリが生成したビットマップは常に4分割される一方、
実データはどんな内容でも常に正しく1回だけ印字される。これは、実データが**単純な1bit生
ビットマップの閾値化ではなく、本物のレンダラー固有の何らかのハーフトーン/エンコード
アルゴリズム**（単なる統計的性質の一致では再現できない、特定の空間的相関構造を持つ処理）を
経ている可能性を強く示唆する。マスクテスト（バイト値のドメインだけを揃える）ではこれを再現できなかったことから、
必要なのは「値の範囲を揃える」ことではなく「実際に同じ
アルゴリズムでハーフトーン/エンコードする」ことだと考えられるが、そのアルゴリズムの
詳細はまだ特定できていない。

## 判定結論（重要・確定）

- Bluetooth方式: **Classic（RFCOMM/SPP）で確定**。BLEではない
- 文字は画像化されているか: **確定（目視確認済み）**。全パターンで文字形状が正しくビットマップとして再現された
- QRは専用コマンドか画像化か: **画像化で確定（目視確認済み）**。ノートエディタ経由のQR印刷も同一ビットマッププロトコルだった
- 採用方式（手順書 30章の優先順位に基づく）: **文字・QRともにPython側で画像化し、共通の1bitビットマッププロトコルで送信する（方式C・D）。** これにより実装は大幅に単純化される
- 残る未確定事項: フッターのチェックサム有無、パケット分割の正確な分割ロジック、写真等の多階調画像の実挙動（文字・QR以外は未検証）
